# Portfolio v2 — PATCH 2026-09-15 23:00 : corrections de relecture + roadmap post-V1

Contexte : le 2026-09-15 à 23:00, Xav a validé l'ensemble du site sur Galaxy A04 (`?perf=lite`) et sur PC plein écran, Playground compris (les 4 cas). La V1 fonctionnelle est complète. Ce patch contient **cinq Micro-Tickets de contenu** (§A à §E, aucun composant modifié sauf pour déplacer une chaîne en dur), une clôture de dette (§F) et la mise à jour du ROADMAP avec les phases post-V1 tranchées par Xav (§G).

Ordre d'exécution : A → B → C → D → E → F → G, puis un seul commit, journal rédigé **avant** le commit, push sur `main` après le go de Xav.

Rappel des contraintes de méthode (ROADMAP) : cause racine avant patch ; aucune chaîne en dur dans un composant (T8) ; vérification visuelle réelle à l'écran ; ne rien inventer.

---

## A. Micro-Ticket — Frise : dates réelles et ordre chronologique

### Constat
`src/content/timeline.ts` : 6 jalons sur 7 ont une période vague (`"2026"`, `"09/2026"`). Le tableau n'est pas dans l'ordre chronologique (Régie Maison en tête alors que haTD commence un mois plus tôt). Le composant `Timeline.tsx` rend le tableau dans l'ordre du fichier.

### Données de Xav (2026-09-15 23:00) — n'afficher **que** ce qui est su
Ordre chronologique par date de début :

| # | id | period (à afficher) | Remarque |
|---|---|---|---|
| 1 | `hatd` | `01/08 – 07/09/2026` | inchangé sinon |
| 2 | `regie-maison` | `28/08 – 01/09/2026` | voir « Premier projet » ci-dessous |
| 3 | `bilan-competences` | `05/09/2026` | |
| 4 | `miniciel` | `07/09/2026` | |
| 5 | `pivot-consultant` | `10/09/2026` | |
| 6 | `templates` | `Depuis le 14/09/2026` | summary : ajouter « En phase de test. » en fin |
| 7 | `portfolio-v2` | `15/09/2026` | |

Format : `JJ/MM/AAAA`, plage avec tiret demi-cadratin ` – ` et l'année une seule fois en fin de plage (comme Xav l'a écrit). Aucune date ajoutée au-delà de ce tableau.

**« Premier projet »** : le summary de Régie Maison dit « Premier projet : … ». Avec les dates réelles, haTD a commencé avant (01/08) mais Régie Maison est livré avant (01/09 vs 07/09). Remplacer par **« Premier projet livré : … »** — exact dans les deux lectures. Ne pas toucher au reste du texte.

### Instructions pour Claude Code
1. Réordonner le tableau `timeline` selon le tableau ci-dessus ; mettre à jour `period` et le summary de `regie-maison` et `templates`. `dateKnown: true` partout (inchangé).
2. `timeline.test.ts` : ajouter un test **« est dans l'ordre chronologique »** qui compare `timeline.map(m => m.id)` à la liste explicite des 7 ids dans l'ordre ci-dessus (pas de parsing de date : la liste est la source de vérité, on la met à jour à la main quand un jalon s'ajoute — le commenter).
3. Vérifier à l'écran (desktop + `?perf=lite`) que les libellés de période longs (`01/08 – 07/09/2026`, `Depuis le 14/09/2026`) ne débordent pas de leur pastille/colonne sur mobile ~500 px. Si ça déborde : c'est un bug de la frise à documenter et corriger à la cause (largeur/`white-space`), pas en raccourcissant la date.
4. Répercuter le tableau des dates dans `specs/06_skills-timeline.md` (section contenu de la frise) et cocher DETTE-12 comme « étendue à tous les jalons » dans `dette_suivi.md` §B (ligne existante, ajouter la mention, ne rien supprimer).

### Critère de validation
7 jalons dans l'ordre ci-dessus, périodes exactes, test vert, aucun débordement à ~500 px.

---

## B. Micro-Ticket — Agent de poche : réponse `roi` (poker chiffré)

### Constat
`src/content/agent.ts`, réponse `roi`, dernière phrase : « … le ROI de l'IA a un potentiel bien plus gros que celui du poker — et je parle en connaissance de cause. » Sans chiffre, la phrase peut se lire comme « j'ai beaucoup perdu ». Xav veut la préciser.

### Texte de Xav (2026-09-15 23:00) — mot pour mot
La phrase finale devient :

> Une chose quand même dont je suis sûr : le ROI de l'IA a un potentiel bien plus élevé que celui du poker, où le ROI moyen varie généralement entre 10 % et 30 % pour un joueur régulier gagnant (souvent moins pour les limites > 100 €) — et je parle en connaissance de cause.

Typographie : espace insécable avant `%`, `>` et `€` (`10 % et 30 %`, `> 100 €`), tiret cadratin ` — ` avant « et je parle », comme dans le reste du fichier.

### Règle levée, règle conservée
- Le commentaire de `agent.ts` (« Ne jamais ajouter de pourcentage ») visait les métriques client (T6). Ce pourcentage décrit le poker, pas un client : **mettre à jour le commentaire** → « Aucun pourcentage qui puisse passer pour une métrique client (T6) ; le seul chiffre autorisé est celui du poker dans `roi`, fourni par Xav. »
- Le test « ne cite jamais le poker en dehors de la réponse roi » (`ScriptedAgentProvider.test.ts`) reste **tel quel** et doit rester vert.

### Instructions pour Claude Code
1. Remplacer la phrase finale de `agentReplies.roi`, sans toucher au reste de la réponse.
2. Mettre à jour le commentaire du bloc comme ci-dessus.
3. Vérifier à l'écran que la réponse plus longue tient dans le panneau de l'agent avec le suivi de bas (`ResizeObserver`) en mode complet et en `?perf=lite`.

### Critère de validation
Texte exact affiché par la puce « Quel est le ROI de tes projets ? », tests verts, aucune autre occurrence de « poker » dans `src/`.

---

## C. Micro-Ticket — Contact : libellé « Contact direct préféré ? » + retour à T8

### Constat
`src/sections/Contact.tsx` ligne 33 : `<p …>Contact direct (préféré)</p>`. Deux problèmes : le libellé voulu par Xav est différent, et **la chaîne est en dur dans un composant** (violation de T8 — tout texte vit dans `src/content/`).

### Instructions pour Claude Code
1. Ajouter dans `src/content/contact.ts` : `export const directContactHeading = "Contact direct préféré ?";` (texte exact, point d'interrogation précédé d'une espace insécable).
2. Remplacer la chaîne en dur de `Contact.tsx` par cet export.
3. Grep de contrôle : `grep -rn "préféré\|Contact direct" src/sections src/components` ne doit plus rien retourner. Profiter du passage pour lister dans le journal (**sans corriger**, scope discipline) toute autre chaîne de contenu en dur trouvée dans `src/sections/*.tsx` par un grep large (`>[A-ZÉ][^<{]{12,}<`) — ce sera un point Phase 7 s'il y en a.

### Critère de validation
Libellé « Contact direct préféré ? » visible au-dessus du numéro et des liens ; aucune chaîne de contenu dans `Contact.tsx`.

---

## D. Micro-Ticket — Agent de poche : les autres LLM (réponse `outils`)

### Constat
La réponse `outils` ne cite que Claude (règle T5b : modèles nommés dans agent/skills). Xav utilise d'autres LLM avec un rôle précis chacun et veut que ça se voie — c'est cohérent avec sa ligne « je choisis l'outil après le problème ».

### Substance fournie par Xav (2026-09-15 23:00)
- Claude (Max) : le principal, « probablement le meilleur dans sa plage d'utilisation ».
- ChatGPT : parfait pour économiser des tokens Claude.
- Gemini : recherches web / Google.
- Perplexity : *la* recherche.
- Mammouth : base centrale quand pas de gros projet en cours (équivalent Claude Pro).
- « et d'autres ».

### Texte proposé (forme rédigée par l'architecte à partir des mots de Xav — **[À RELIRE — Xav]**, non bloquant)
Réponse `outils` complète, remplace l'actuelle :

> Claude au quotidien — Fable 5.1, Opus 5, Sonnet 5 selon la tâche — et Claude Code piloté par des specs écrites : c'est mon outil principal, probablement le meilleur dans sa plage d'utilisation. Mais pas le seul : ChatGPT pour les tâches courantes et économiser mes tokens Claude, Gemini pour ce qui touche au web et à Google, Perplexity pour la recherche documentaire, Mammouth comme base centrale entre deux gros projets — et d'autres selon le besoin. Python, JS, PowerShell pour le reste. Automatisation (Make, n8n) et RAG : notions pour l'instant, à creuser quand un projet le demandera vraiment. Je choisis l'outil après le problème, jamais avant.

### Skills matrix (même patch, famille « LLMs & agents »)
Ajouter un badge après la ligne Claude :
`{ family: "LLMs & agents", name: "Autres LLM (ChatGPT, Gemini, Perplexity, Mammouth)", level: 3, note: "usage ciblé : chacun pour ce qu'il fait le mieux" }`
Niveau 3 = valeur par défaut posée par l'architecte ; **Xav l'ajuste directement dans `skills.ts` s'il le souhaite**, comme pour DETTE-15. `skills.test.ts` doit rester vert (famille à ≥ 1 axe, ids/noms uniques).

### Instructions pour Claude Code
1. Remplacer `agentReplies.outils` par le texte ci-dessus. Vérifier que les mots-clés de repli de la saisie libre (`chatgpt`, `gemini`, `perplexity`, `mammouth`) routent vers `outils` — les ajouter à la table de mots-clés si elle existe, sinon le noter.
2. Ajouter le badge dans `skills.ts` ; vérifier à l'écran le rendu du badge (libellé long) dans la famille sur desktop et ~500 px — même règle que §A.3 : débordement = bug de primitive, pas de raccourcissement du nom.
3. Marquer `[À RELIRE — Xav]` en commentaire au-dessus de `outils` ; ajouter la ligne dans `dette_suivi.md` §B comme **DETTE-32** : « Relecture de la réponse `outils` (autres LLM) et du niveau du badge — texte rédigé par l'architecte à partir des mots de Xav, non bloquant. »

### Critère de validation
Réponse affichée par la puce « Quels outils tu utilises ? », badge visible, tests verts.

---

## E. Micro-Ticket — Agent de poche : barre de défilement (reliquat Phase 3)

### Constat
Noté dans `JOURNAL_DEV.md` Phase 3 : l'utilitaire `scrollbar-subtle` a été créé pour le Playground ; l'agent de poche a le même conteneur scrollable avec la barre claire de Chrome. Prévu Phase 7, mais c'est une classe à ajouter — autant le faire dans ce passage de contenu plutôt que de le traîner.

### Instructions pour Claude Code
1. Appliquer `scrollbar-subtle` au conteneur scrollable de `AgentPanel.tsx` (celui qui porte le `ResizeObserver`).
2. Vérifier à l'écran : Chrome desktop (barre discrète), `?perf=lite` inchangé, aucune régression du suivi de bas pendant la frappe.
3. Retirer la mention « à appliquer en Phase 7 » du ROADMAP (§G) — le journal Phase 3 reste tel quel (historique).

---

## F. Clôture de dette

`dette_suivi.md` §B — DETTE-09 : ⏸ → ☑ « Textes des 4 cas relus et validés par Xav le 2026-09-15 (23:00), cas *bizarre* compris. » Retirer le marqueur `[À RELIRE — Xav]` en tête de `src/content/playground.ts` (remplacer par « Textes validés par Xav le 2026-09-15 »).

`dette_suivi.md` §D — ajouter : `2026-09-15 23:00 — Site validé par Xav sur Galaxy A04 (perf=lite) et PC plein écran, toutes phases. Patch de relecture (PATCHES_2026-09-15_2300.md) : dates de la frise, poker chiffré, libellé Contact (+ chaîne en dur sortie de Contact.tsx, T8), autres LLM (DETTE-32 ouverte), scrollbar agent. DETTE-09 close. Roadmap post-V1 tranchée (§G).`

---

## G. ROADMAP — phases post-V1 tranchées par Xav (2026-09-15 23:00)

Mettre `specs/00_ROADMAP.md` en **version 0.8.0**. Statut : « V1 fonctionnelle complète et validée par Xav (toutes phases 0-6b, 2, 3). Patch 23:00 appliqué. Phase courante : **7** (spec à écrire). »

Remplacer la ligne « Phase 7 » de l'esquisse et ajouter les phases suivantes. **Ordre imposé** — l'anglais vient en dernier parce que Xav ne veut le faire que « quand je n'ai plus envie de changer quoi que ce soit » : chaque phase qui ajoute du contenu (CV, FAQ) doit donc précéder la traduction.

- **Phase 7 — Polish, perf, SEO, mentions légales** (spec à écrire — Module Standard) : page Mentions légales complète (identité, SIRET, hébergeur GitHub, RGPD du formulaire — obligation légale pour un site professionnel, à ne pas repousser), SEO/OG/sitemap/JSON-LD Person sur l'URL actuelle `xab-dev.github.io/portfolio-v2/` (domaine personnalisé : **statu quo**, décision de Xav, à réévaluer avec les finances), vérification et liens des 4 références (DETTE-31), largeur des panneaux du Playground (longues lignes), audit `prefers-reduced-motion` avec un vrai outil d'émulation, Lighthouse à reconfirmer. **L'image OG définitive attend le « skin » perso** (voir post-V1) : en Phase 7, OG = avatar + titre, sobre.
- **Phase 8 — Export CV classique (PDF une page)** : généré depuis `src/content/*.ts` (projets, skills, timeline, contact) pour ne jamais diverger du site ; bouton « Télécharger le CV » dans le Hero et le Contact ; génération à la build (fichier statique dans `dist/`) plutôt qu'à la volée côté client, pour le poids et le mode allégé. Spec à écrire.
- **Phase 9 — FAQ « Comment ce site a été créé ? »** : bouton en haut à droite de la section Jouer, **absent de la nav et du flux principal** ; ouvre un pop-up (primitive `Modal`) contenant une **discussion factice** au format de l'agent de poche (`AgentMessage`/`TypingText`) qui répond à la question principale — le site lui-même comme cas réel : specs numérotées, Claude Code, phases, dette suivie, vérification visuelle, mode allégé après test terrain. Contenu dans `src/content/faq.ts`, dérivé de `JOURNAL_DEV.md` (rien d'inventé). Spec à écrire.
- **Phase 10 — Version anglaise** : à lancer seulement une fois les phases 7-9 livrées et **actées par Xav** (« plus envie de changer quoi que ce soit »). Toute l'architecture T8 (`src/content/`) est faite pour ça ; la spec fixera le mécanisme (fichiers `*.en.ts` + sélecteur, ou sous-chemin `/en/`). haTD reste en français.
- **Post-V1, non planifié** (décisions de Xav) : Playwright e2e (après la version anglaise), domaine personnalisé (statu quo, budget), mesure d'audience (pas prioritaire — « la mesure réelle se fait sur les contrats signés »), image OG / « skin » perso (quand tout sera stable et intégré), agent de poche V2 branché sur une API (passe son tour).

Ordre : patch 23:00 → 7 → 8 → 9 → 10. **Design et animations : figés**, on n'y touche plus (validés par Xav).

---

## Prompt de lancement (à coller dans Claude Code)

> Lis `specs/PATCHES_2026-09-15_2300.md` en entier, puis exécute les unités A à G dans l'ordre. Ce sont des corrections de contenu : aucun composant n'est modifié sauf pour sortir une chaîne en dur (§C) et ajouter une classe (§E). Vérifie chaque critère de validation à l'écran (desktop et `?perf=lite`, mobile ~500 px), rédige l'entrée `JOURNAL_DEV.md` **avant** de committer, un seul commit, et arrête-toi avant le push pour mon go.
