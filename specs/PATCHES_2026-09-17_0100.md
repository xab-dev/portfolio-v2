# Patch de relecture 9b — 2026-09-17 01:00

**Nature** : patch de contenu et de petites corrections, pas une phase. Clôt la **Phase 9** (critère §7.8 de `specs/10_faq-comment-ce-site.md` : relecture faite par Xav le 17/09 01:00, remarques ci-dessous ; tout ce qui n'est pas listé est validé tel quel, bas de page compris).

**Décisions prises par Xav (17/09) — ne pas rediscuter** :
- Agent de poche : **solution 1** (bloquer les puces déjà posées). Mesure d'usage via un tiers **écartée** (DETTE-37). Jonction des questions de l'agent au mail de contact **rejetée** (risque de données perso dans la saisie libre — DETTE-38, à borner plus tard).
- Compétences : « workflow » va bien dans No-code, nommé sans ambiguïté ; Autres LLM 3 → 4 (note Xav du 16/09, DETTE-32) ; Langues devient un badge chiffré 5/5 (annule DETTE-21) ; SQL 2/5.
- Portfolio v2 en fiche projet, statut `en production` (« v2 » est le nom du site, pas sa version — le site est en V1 selon le ROADMAP ; le type `ProjectStatus` ne change pas).
- **D7 (CV PDF)** : voir §8.
- Gmail secondaire : décision du 16/09 jamais poussée sur `main` — retrait effectif dans ce patch.

**Contraintes de méthode inchangées** : cause racine avant patch, vérification visuelle réelle à 375 px et 1280 px, `npm run test` / `lint` / `build` verts, scope discipline (rien d'autre que ce qui est listé), journal + dette. Textes UI en français, identifiants en anglais. Espaces insécables devant `?`, `:`, `»`, `%` comme partout.

---

## 1. Hero

`src/content/hero.ts` : `ctaSecondary.label` → **« Qualifier votre besoin »** (cohérent avec le sous-titre Contact « Qualifiez votre besoin en 3 clics »).

## 2. Agent de poche — puces déjà posées

**Comportement attendu**
- Une puce dont la réponse a **déjà été donnée dans la conversation courante** est désactivée : `disabled`, `aria-disabled="true"`, icône `Check` (Lucide) à gauche du libellé, style visuellement atténué mais lisible (pas d'opacité < 0,5). Tooltip/`title` : « Déjà répondu — voir plus haut ».
- Une réponse compte comme « donnée » quel que soit le chemin : clic sur la puce **ou** saisie libre qui aboutit au même id via `agentKeywords`. Pour cela, `matchReply` retourne `{ id?: string; text: string }` (`id` absent pour `agentDefaultReply`) — `AgentReply` étendu, `ScriptedAgentProvider.test.ts` adapté.
- La saisie libre reste toujours possible ; une saisie libre qui retombe sur un id déjà consommé **redonne la réponse normalement** (pas de nouveau texte à rédiger, T7).
- Le bouton « Réinitialiser la conversation » devient visible **dès qu'au moins une puce est consommée** (et non plus seulement à `MAX_HISTORY`). Réinitialiser vide aussi l'ensemble des ids consommés.
- État en mémoire React uniquement (`useState<Set<string>>`) : rien dans `sessionStorage`, rien envoyé nulle part.
- Reduced-motion / mode allégé : aucun effet nouveau.

**Fichiers** : `src/components/agent/AgentPanel.tsx`, `src/lib/agent/AgentProvider.ts` (type), `src/lib/agent/ScriptedAgentProvider.ts` (+ test).

**Critère** : cliquer 5 puces → 5 réponses, 5 puces désactivées, bouton Réinitialiser présent ; taper « roi » en saisie libre après avoir cliqué la puce ROI → réponse redonnée, puce toujours désactivée ; Réinitialiser → tout réactivé.

## 3. Projets (`src/content/projects.ts`)

### 3.1 `hatd` — mise à jour
- `status` : `"en développement"` → `"v1 fonctionnelle"`.
- `tagline` : « Tower defense clicker solo, avec surcouche RPG (V1 terminée) — test UX en cours. »
- `architecture`, dernière ligne (« Cible à terme : portage sur Godot 4… ») → **remplacer** par : « La suite est la refonte RPG-v2, restée en HTML5/Canvas — voir sa fiche. » (la migration Godot a été abandonnée par Xav ; garder l'ancienne ligne serait faux).
- `metrics`, ligne playtest → `{ label: "Playtest externe", value: "1 (16/09/2026, 3 h)", verified: true }` — le marqueur `[À CONFIRMER date]` disparaît.
- `limits`, première ligne → « Équilibrage et test UX en cours (playtest externe du 16/09/2026). »

### 3.2 `templates` — métrique DETTE-11
`{ label: "Sessions où les templates ont servi", value: "[DETTE-11]", verified: false }` → `{ label: "Projets pilotés avec les templates", value: "2 (portfolio v2, RPG-v2)", verified: true }`. Note Xav : le bilan chiffré sera publié avec les versions finales — ajouter cette phrase en fin de `limits`.

### 3.3 Nouvelle fiche `rpg-v2` — **[ARRÊT XAV] sur le texte** (rédigé par l'architecte à partir des mots de Xav, rien d'inventé ; Xav relit et corrige directement dans le fichier, comme pour DETTE-15)
```ts
{
  id: "rpg-v2",
  title: "RPG-v2",
  tagline: "Refonte complète d'un RPG médiéval-fantastique en HTML5/Canvas, la V1 servant de prototype jetable.",
  tags: ["Jeu"],
  status: "en développement",
  year: 2026,
  problem:
    "Repartir d'une V1 jouée de bout en bout (tous les boss battus) pour construire un jeu commercialisable — et en faire une vitrine de ce que la génération de jeux assistée par IA permet à un développeur seul.",
  architecture: [
    "HTML5/JS/Canvas, résolution logique 480 × 270 : le choix du moteur natif (Godot) a été écarté faute de pouvoir tenir seul un rôle de graphiste à plein temps.",
    "Périmètre fermé à la première ère (Grotte → Maison → première zone → Château → Boss 1) ; 3 éléments ; bilingue FR/EN ; entièrement jouable hors-ligne.",
    "PC à la manette comme plateforme primaire, mobile mené en parallèle. Gratuit + dons : ni publicité ni achat intégré, non négociable.",
    "Roadmap par phases + specs numérotées + CLAUDE.md, exécutées par Claude Code — même méthode que ce site. Phase 0 (socle) et Phase 1 (Grotte, cinématique et direction artistique validées) closes ; Phase 2 (Maison) en cours.",
    "haTD devient un mini-jeu du monde, tout en restant jouable séparément. Bande son composée au piano par Xav.",
  ],
  metrics: [
    { label: "V1 terminée de bout en bout", value: "oui", verified: true },
    { label: "Phases closes (V2)", value: "2 sur la roadmap", verified: true },
  ],
  limits: [
    "Contrôles tactiles encore imparfaits — dette assumée jusqu'à l'intégration des compétences.",
    "Pas encore de lien de partage public : le test sur téléphone est différé.",
    "Aucune touche n'est montrée au joueur pour l'instant — point ouvert, volontairement pas tranché.",
  ],
  accent: "blue",
}
```
Position dans le tableau : juste après `hatd`. Pas d'image, pas de lien (rien de public).

### 3.4 Nouvelle fiche `portfolio-v2` (contenu tiré de `faq.ts` / `JOURNAL_DEV.md`, T6/T7)
```ts
{
  id: "portfolio-v2",
  title: "Portfolio v2 — ce site",
  tagline: "Vous l'avez sous les yeux.",
  tags: ["Outils", "Méthode"],
  status: "en production",
  year: 2026,
  problem:
    "Pas de CV à jour, pas de réseaux, rien qui montre ce que je veux faire : un problème d'exposition, pas un problème de code.",
  architecture: [
    "React + Vite + Tailwind + Motion, déployé sur GitHub Pages par GitHub Actions ; tout le contenu dans `src/content/*.ts`.",
    "11 specs numérotées, une phase par spec, exécutées par Claude Code : cause racine avant patch, vérification visuelle réelle, journal et dette à chaque phase.",
    "CV PDF généré à la build depuis la même source de contenu — le build refuse de passer s'il dépasse une page.",
    "Mode allégé pour mobile (`?perf=lite`), rendu PC identique au pixel près.",
    "Agent de poche scripté, simulateur aux hypothèses affichées et sourcées, Prompt Playground basé sur mes templates de spec.",
  ],
  metrics: [
    { label: "De la dictée à la mise en ligne", value: "3 jours", verified: true },
    { label: "Specs numérotées", value: "11", verified: true },
    { label: "Tests automatisés", value: "<nombre réel après ce patch>", verified: true },
  ],
  limits: [
    "Agent scripté, pas de LLM branché : choix assumé, une V2 est envisagée.",
    "Aucun analytics — la mesure se fait sur les contrats signés.",
    "`noindex` jusqu'au polish final.",
  ],
  links: [{ label: "Code source", href: site.links.github }],
  accent: "violet",
}
```
Position : en tête du tableau (c'est le cas réel le plus visible). `<nombre réel après ce patch>` = valeur lue en fin de session, jamais un chiffre supposé.

## 4. Frise (`src/content/timeline.ts`)
- Jalon `portfolio-v2` existant : `period` → `"15 – 16/09/2026"`, `summary` → « Portfolio/CV interactif piloté par specs, livré phase par phase — en ligne. » (ne pas créer un second jalon).
- Nouveau jalon, **après** `portfolio-v2` (ordre chronologique par date de début, test existant) :
```ts
{ id: "rpg-v2", period: "Depuis le 16/09/2026", title: "RPG-v2", summary: "Refonte complète du RPG en HTML5/Canvas, pilotée par specs — Grotte close, Maison en cours.", kind: "projet", dateKnown: true }
```

## 5. Compétences (`src/content/skills.ts`)
- No-code / automatisation : ajouter `{ name: "Conception de workflows humain ↔ LLM ↔ agent de code", level: 5, note: "la logique, pas la plateforme no-code" }`. Effet accepté par Xav : radar No-code 1 → 3.
- LLMs & agents, « Autres LLM » : `level: 3` → `4` ; supprimer le commentaire « Niveau 3 posé par l'architecte… ». DETTE-32 close.
- Données : ajouter `{ name: "SQL", level: 2, note: "notions correctes — requêtes sur bases de hand histories poker (trackers, échantillons de 50 000 à plus d'un million de mains)" }`. Radar Données 1 → 1,5.
- Humain : ajouter `{ name: "Langues : Français / English", level: 5 }` ; **supprimer** `unleveledSkills`, l'interface `UnleveledSkill` et le commentaire DETTE-21 ; adapter tous les consommateurs (grep `unleveledSkills` : matrice, tests, `buildCvModel.ts`). Radar Humain 3 → 4. Le CV PDF garde `site.languages` comme unique source pour les langues (pas de doublon avec le badge).
- Vérifier `skills.test.ts` (chaque famille ≥ 1 axe) et le rendu radar à 375 px après ces ajouts.

## 6. Contact
### 6.1 Nouveau type « Créer un site » (`src/content/contact.ts`, `ContactForm.tsx`)
- `projectTypes` : insérer `"Créer un site (vitrine ou comme celui-ci)"` avant `"Autre"`.
- Nouveau contenu :
```ts
export const siteQuoteTiers = [
  { label: "Site vitrine simple", price: "à partir de 600 €" },
  { label: "Un site comme celui-ci", price: "environ 1 500 €" },
  { label: "Plus ambitieux que celui-ci", price: "à partir de 2 000 €" },
];
export const siteQuoteNote = "Repères hors nom de domaine et hébergement. Devis après un premier échange.";
```
- Affichage : quand ce type est sélectionné à l'étape 1, un encart compact (3 lignes + note) apparaît sous les tags, et reste visible en haut de l'étape 2 (Budget). Aucun autre type n'affiche d'encart. Les paliers tombent dans les fourchettes existantes, rien à changer côté `budgets`.
- Le payload Formspree ne change pas de forme (`projectType` porte le nouveau libellé).

### 6.2 Retrait définitif du Gmail secondaire
- `site.ts` : supprimer `contact.emailSecondary`. `contact.ts` : supprimer `directContact.emailSecondary`. `Contact.tsx` : supprimer le second lien `mailto`.
- `grep -rn "phenomenxx" .` sur tout le repo (JSON-LD, mentions légales, `buildCvModel.ts` / `CvDocument.tsx`, scripts, tests, specs, dette) : **zéro occurrence** dans le code et le contenu à la fin ; dans `dette_suivi.md` DETTE-04, remplacer la mention par « Gmail secondaire retiré (17/09) ».

## 7. Section Jouer (`src/content/play.ts`, `src/sections/Play.tsx`)
- `play.title` → `"Jouer"` ; nouvelle clé `play.heading: "haTD, mon tower defense (v1 en ligne) :"` ; `play.intro` inchangé.
- `SectionShell` : `title={play.title}` **sans** `subtitle` ; `headerAction` (bouton FAQ) inchangé, même position.
- Dans le corps, dans cet ordre : `<h3>` = `play.heading` (style de titre de paragraphe, `font-display`), `<p>` = `play.intro`, puis le lien « Voir la fiche projet », puis le cadre du jeu / bouton « Voir l'intro » comme aujourd'hui.
- Résultat attendu :
```
Jouer                                   (Comment ce site a été créé ?)

haTD, mon tower defense (v1 en ligne) :
Prototype jouable en ligne, développé seul…
Voir la fiche projet
```
- Vérifier que la nav et `#jouer` ne dépendaient pas du sous-titre.

## 8. CV PDF — règle D7
Le PDF tenait sur une page en Phase 8 avec 6 projets et 7 jalons, marges déjà resserrées. Ce patch ajoute 2 projets, 1 jalon et 3 compétences : **le build va probablement passer au rouge, c'est attendu.**
1. Jouer d'abord les leviers de mise en page de la spec 09 (§4-1).
2. Si le PDF dépasse encore une page : `buildCvModel.ts` **exclut du PDF les projets en statut `"cadrage"`** (Séquence terrain, 1AM) — règle codée et testée, pas une exception manuelle. Le site n'est pas concerné. Ordre de priorité si un choix devait encore se poser : `en production` > `v1 fonctionnelle` > `en développement` > `cadrage`, puis date décroissante.
3. Si ça déborde toujours : **[ARRÊT XAV]**, ne rien retirer d'autre.
Documenter dans `JOURNAL_DEV.md` quel palier a suffi. Ajouter D7 tranchée au tableau des décisions du ROADMAP.

## 9. FAQ (`src/content/faq.ts`) — chiffres dérivés
La FAQ est dérivée du journal, elle ne doit pas mentir : en **fin** de session, mettre à jour les nombres qui ont bougé — « 36 lignes de dette suivies » / « 36 lignes numérotées à ce jour » (§creation, §dette), « 81 tests » (§creation), « Ce site en a connu sept » (§arret, +1 avec l'ARRÊT XAV de la fiche RPG-v2). Valeurs réelles lues dans le repo, pas estimées. Rien d'autre ne change dans la FAQ.

## 10. Documents du repo

### `specs/00_ROADMAP.md` → v0.9.2
- En-tête : ajouter « …, puis le 2026-09-17 01:00 (relecture 9b par Xav → Phase 9 close, patch `PATCHES_2026-09-17_0100.md`, D7 tranchée) ».
- Statut : `Phase 9 close` (relecture §7.8 faite le 17/09). Patch de relecture 9b exécuté. Prochaine phase : **10 (version anglaise, spec 11)**, à lancer seulement quand Xav n'a plus envie de rien changer ; Playwright après.
- Tableau des décisions : ajouter `| D7 | Débordement du CV PDF | Tranché (17/09) : leviers de mise en page, puis exclusion des projets en statut cadrage du PDF seulement (§8 du patch 9b). |`.

### `specs/02_hero-agent-poche.md`
- §3 comportement : ajouter « Puces déjà posées désactivées (coche), réactivées par Réinitialiser — patch 9b, 17/09 ».

### `specs/06_skills-timeline.md`
- Là où DETTE-21 fixe « Langues = badge sans niveau » : annoter « **annulé le 17/09 (patch 9b)** : Langues devient un badge chiffré 5/5, inclus dans le radar Humain ».

### `specs/07_contact-jouer.md`
- §2 `projectTypes` : ajouter le nouveau type ; §3 Contact : ajouter l'encart `siteQuoteTiers` ; §3 Contact direct : retirer `phenomenxx@gmail.com` ; §3 Jouer : titre → « Jouer », ligne « haTD, mon tower defense (v1 en ligne) : » en titre de paragraphe.

### `dette_suivi.md` (l'agent ajoute, Xav coche — sauf lignes explicitement closes ici)
- DETTE-04 : retirer le Gmail de la ligne (voir §6.2).
- DETTE-11 : ☐ → ☑, réponse « 2 projets (portfolio v2, RPG-v2), métrique affichée ; bilan chiffré aux versions finales ».
- DETTE-32 : ☐ → ☑, « Autres LLM 3 → 4 appliqué (patch 9b) ; réponse `outils` validée par la relecture 9b ».
- DETTE-21 : annoter « annulée le 17/09, Langues chiffrées 5/5 ».
- Nouvelles lignes §C :
  - `☐ [DETTE-37] Mesure d'usage de l'agent de poche (quelles puces sont lues) : écartée le 17/09 — site statique sans backend, impliquerait un tiers (nouveau sous-traitant, registre CNIL et mentions légales à réviser). À retrancher avec la question du domaine perso. | Hors phase`
  - `☐ [DETTE-38] Joindre au mail de contact les questions posées à l'agent : rejetée le 17/09 (risque de données personnelles dans la saisie libre). Borne possible à étudier : ne transmettre que les ids de puces, jamais la saisie libre. | Hors phase`
  - `☐ [DETTE-39] Texte de la fiche RPG-v2 rédigé par l'architecte à partir des mots de Xav — relecture Xav requise (édition directe dans projects.ts). | Patch 9b — [ARRÊT XAV]`
- §D : entrée datée 2026-09-17 résumant le patch (fichiers touchés, D7, palier retenu pour le PDF, chiffres FAQ mis à jour, nombre de tests).

---

## Prompt de lancement (à coller dans Claude Code)

> Lis `specs/00_ROADMAP.md` en entier (v0.9.1), puis `PATCHES_2026-09-17_0100.md` en entier et exécute-le intégralement, sections 1 à 10, dans l'ordre. C'est un patch de contenu qui clôt la Phase 9 : scope strict, rien d'autre. Points d'attention : (a) `grep -rn phenomenxx .` doit être vide à la fin ; (b) le build du CV PDF va probablement passer au rouge après les ajouts — applique la règle D7 (§8) palier par palier et note lequel a suffi ; (c) les chiffres de la FAQ (§9) et la métrique « Tests automatisés » de la fiche portfolio-v2 se remplissent avec les valeurs réelles lues en fin de session ; (d) le texte de la fiche RPG-v2 est un [ARRÊT XAV], tu le poses tel quel et tu le signales. Vérification visuelle réelle à 375 px et 1280 px : agent (puces désactivées + Réinitialiser), Contact (encart « Créer un site » aux étapes 1 et 2, plus de Gmail), Jouer (nouveau titre/paragraphe, bouton FAQ toujours en haut à droite), radar (3 nouveaux badges), frise (jalon RPG-v2 en dernier). Vérifie aussi si la branche `master` existe encore sur le remote : signale-le, ne la supprime pas toi-même. `npm run test`/`lint`/`build` verts, ROADMAP 0.9.2, journal et dette à jour, puis compte-rendu au format de fin de session. Arrête-toi avant tout push si un ARRÊT XAV autre que DETTE-39 apparaît.
