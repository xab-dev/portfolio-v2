import { SectionShell } from "../components/ui/SectionShell";

export function Play() {
  return (
    <SectionShell id="jouer" title="Jouer" subtitle="Contenu en Phase 6.">
      <p className="text-text-muted">Intégration de haTD via iframe à venir.</p>
    </SectionShell>
  );
}
