import { SectionShell } from "../components/ui/SectionShell";

export function Portfolio() {
  return (
    <SectionShell id="projets" title="Projets" subtitle="Contenu en Phase 4.">
      <p className="text-text-muted">Grille de projets filtrable à venir.</p>
    </SectionShell>
  );
}
