import { SectionShell } from "../components/ui/SectionShell";

export function Hero() {
  return (
    <SectionShell id="hero" title="Accueil" subtitle="Contenu en Phase 1.">
      <p className="text-text-muted">Accroche, puces de questions et agent de poche à venir.</p>
    </SectionShell>
  );
}
